<div>

    

    <h1><%= @post.titulo %></h1>
    <h2><%= @post.subtitulo %></h2>

        <% if @post.imagem.present? %>
            <img src="<%= @post.imagem %>" alt="<%= @post.titulo %>" style="max-width: 600px;">
    <% end %>

    <p><%= @post.descricao %></p>

    <p>
        Site do parceiro: 
    <%= link_to "Clique aqui", @post.link, target: "_blank" %>
    </p>

    <%= link_to "Voltar para a Home", root_path %>

</div>
